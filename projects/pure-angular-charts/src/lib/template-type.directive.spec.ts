import { TemplateTypeDirective } from './template-type.directive';

describe('TemplateTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateTypeDirective();
    expect(directive).toBeTruthy();
  });
});
